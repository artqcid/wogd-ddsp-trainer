#!/bin/sh
# Post-commit hook: auto-sync the wiki/RAG index after every commit.
# Activated via `git config core.hooksPath .githooks`.

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT" || exit 1
python scripts/sync-wiki.py